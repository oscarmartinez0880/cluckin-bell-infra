apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluckin-bell-app.fullname" . }}
  labels:
    {{- include "cluckin-bell-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluckin-bell-app.selectorLabels" . | nindent 4 }}